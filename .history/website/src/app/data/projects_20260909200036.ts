

type Project ={
    number: string;
    title: string;
    description: string;
    tags: string[];
    imageUrl?: string;
    link?: string;
}

export default function  Project({number, title, description, tags, imageUrl, link}: Project){

}